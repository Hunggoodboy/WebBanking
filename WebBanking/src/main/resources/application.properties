spring.application.name=BankingEconomy
# Cấu hình kết nối MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/webbanking?useSSL=false&serverTimezone=UTC&createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=170205

# Cấu hình JPA/Hibernate (Tự động tạo bảng)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.docker.compose.enabled=false
# Tạm thời tạo tài khoản để test API
spring.security.user.name=hao
spring.security.user.password=123456
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration